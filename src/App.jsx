import { BrowserRouter, Routes, Route } from "react-router-dom";
import Template from "./template/Template";

function App() {
  return (
    <BrowserRouter>
        <Template>
          <Routes>

          </Routes>
        </Template>
    </BrowserRouter>
  )
}

export default App
