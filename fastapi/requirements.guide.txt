# guide 서비스 전용(무거움). embed 의 requirements.txt 와 *별도*.
# torch/torchvision 은 Dockerfile.guide 에서 CPU wheel 로 설치(여기서 제외).

fastapi==0.136.1
uvicorn[standard]==0.46.0
pydantic==2.13.3
pydantic-settings
python-multipart==0.0.29
python-dotenv==1.2.2
pillow==11.3.0
pillow-heif              # normalize.py 최상위 — HEIC/HEIF 디코드(아이폰 업로드)
numpy==2.2.5
PyYAML==6.0.2                   # diagnose.py·router.py 최상위 — taxonomy/track YAML
requests                 # llm.py(Grok)·vision.py(Gemini) 최상위 — artref 는 requests 사용

# 가이딩 핵심
open_clip_torch          # OpenCLIP ViT-L/14 (지연 로드)
qdrant-client            # 가이딩 ref 검색(reference_images_dev)
SQLAlchemy               # drawe_guide (성장/로그). DSN 스킴에 맞춰 드라이버 선택
PyMySQL                  # DB_DSN = mysql+pymysql://...
boto3                    # S3(에셋 해소·presign)
mediapipe==0.10.14                # 손/포즈(게이트). aarch64 휠은 P5 빌드에서 확인
opencv-python-headless
google-auth              # vision.py vertex 백엔드(ADC 토큰 발급). VLM_BACKEND=vertex 일 때만 사용

# 지연/게이트라 우리 구성에선 생략:
#   pinecone(VECTOR_BACKEND=pinecone) · rembg(SUBJECT_MASK) · redis(REDIS_URL)

# Observability
#  embed 는 1.29.0/0.50b0. guide 는 mediapipe==0.10.14(protobuf<5) 와의 충돌 때문에
#  protobuf<5 를 유지하는 마지막 OTel 라인(1.27.0/0.48b0)으로 핀. 1.28.0+ 는 protobuf>=5 요구.
opentelemetry-distro==0.48b0
opentelemetry-exporter-otlp==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
redis==5.2.1
