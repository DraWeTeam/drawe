services:
  frontend:
    build: .
    ports:
      - "5173:80"
    environment:
      - VITE_API_URL=http://localhost:8080
    networks:
      - app-network

networks:
  app-network:
    external: true
