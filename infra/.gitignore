# Terraform
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/*.tfvars
!**/*.tfvars.example
**/crash.log
**/.terraform.lock.hcl
**/.build/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store
Thumbs.db

# Secrets
.env
.env.*
!.env.example
*.pem
*.key

# Backup
*.bak
*.swp

*.sql
beta_snapshot
extract_beta_snapshot.py
*.bak-*

# Local-only compose overrides (머신 종속: 개인 .aws 경로 등 — 커밋 금지)
docker-compose.bedrock.local.yml
