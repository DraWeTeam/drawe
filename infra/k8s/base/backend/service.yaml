apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
spec:
  # ALB Ingress 가 target-type: instance 라서 NodePort 필요
  type: NodePort
  selector:
    app: backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
