apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fastapi-embed
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/DraWeTeam/drawe.git
    targetRevision: main
    path: infra/k8s/overlays/prod/fastapi-embed
  destination:
    server: https://kubernetes.default.svc
    namespace: drawe-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
