name: FastAPI Guide CD

# [eks-gitops-reform] dev(develop)=EKS ArgoCD GitOps / prod(main)=EKS ArgoCD GitOps
on:
  push:
    branches: [develop, main]
    paths:
      - "fastapi/guide/**"
      - "fastapi/assets/**"
      - "fastapi/Dockerfile.guide"
      - "fastapi/requirements.guide.txt"
  workflow_dispatch:
    inputs:
      environment:
        description: 'Deploy target'
        required: true
        type: choice
        options: [dev, prod]
        default: dev

permissions:
  id-token: write
  contents: write

jobs:
  resolve-env:
    runs-on: ubuntu-latest
    outputs:
      env_name: ${{ steps.resolve.outputs.env_name }}
    steps:
      - id: resolve
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            echo "env_name=${{ github.event.inputs.environment }}" >> $GITHUB_OUTPUT
          elif [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "env_name=prod" >> $GITHUB_OUTPUT
          else
            echo "env_name=dev" >> $GITHUB_OUTPUT
          fi

  deploy:
    needs: resolve-env
    runs-on: ubuntu-latest
    environment: ${{ needs.resolve-env.outputs.env_name }}

    env:
      AWS_REGION: ap-northeast-2
      ENV_NAME: ${{ needs.resolve-env.outputs.env_name }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set up QEMU (ARM64 cross-build)
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::${{ secrets.AWS_ACCOUNT_ID }}:role/drawe-${{ env.ENV_NAME }}-github-deploy-role
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build & push Docker image (linux/arm64, Dockerfile.guide)
        id: build
        working-directory: fastapi
        env:
          REGISTRY: ${{ steps.ecr.outputs.registry }}
          REPO: drawe-${{ env.ENV_NAME }}-fastapi-guide
          TAG: ${{ github.sha }}
        run: |
          docker buildx build \
              --platform linux/arm64 \
              -f Dockerfile.guide \
              --build-arg HF_TOKEN="${{ secrets.HF_TOKEN }}" \
              -t $REGISTRY/$REPO:$TAG \
              -t $REGISTRY/$REPO:latest \
              --push \
              .
          echo "image_uri=$REGISTRY/$REPO:$TAG" >> $GITHUB_OUTPUT

      # ───────── DEV: EKS GitOps (ArgoCD) ─────────
      - name: "[dev] Bump overlay newTag → commit (ArgoCD GitOps)"
        if: env.ENV_NAME == 'dev'
        env:
          OVERLAY: infra/k8s/overlays/dev/fastapi-guide/kustomization.yaml
          SHA: ${{ github.sha }}
        run: |
          sed -i -E "s|(newTag:[[:space:]]*)[A-Za-z0-9._-]+|\1${SHA}|" "$OVERLAY"
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$OVERLAY"
          if git diff --cached --quiet; then
            echo "newTag 이미 ${SHA} — 커밋 없음"; exit 0
          fi
          git commit -m "ci(dev): bump fastapi-guide image -> ${SHA} [skip ci]"
          # 동시 push 경합(race) 대비 — rebase 후 재시도 (최대 5회).
          # 다른 CD 가 먼저 main 을 옮기면 push 가 non-fast-forward 로 거부되므로,
          # 내 bump 커밋은 유지한 채 origin 을 rebase 로 당겨 위로 얹고 다시 push.
          REF="${{ github.ref_name }}"
          git config pull.rebase true
          for attempt in 1 2 3 4 5; do
            if git push origin "HEAD:${REF}"; then
              echo "✅ overlay 갱신 푸시 — ArgoCD(develop 추적)가 롤아웃합니다."
              exit 0
            fi
            echo "⚠ push 거부(경합 추정) — 재시도 ${attempt}/5: origin/${REF} rebase"
            if ! git pull --rebase origin "${REF}"; then
              # 같은 overlay 를 양쪽이 건드려 충돌 → 우리 newTag(${SHA})로 해소
              echo "rebase 충돌 — newTag ${SHA} 로 재적용"
              sed -i -E "s|(newTag:[[:space:]]*)[A-Za-z0-9._-]+|\\1${SHA}|" "$OVERLAY"
              git add "$OVERLAY"
              GIT_EDITOR=true git rebase --continue || git rebase --abort
            fi
            sleep $((RANDOM % 5 + 2))   # 2~6초 지터로 동시 재시도 분산
          done
          echo "✘ 5회 재시도 후에도 push 실패 — 수동 확인 필요"; exit 1

      # ───────── PROD: EKS GitOps (ArgoCD) ─────────
      - name: "[prod] Bump overlay newTag → commit (ArgoCD GitOps)"
        if: env.ENV_NAME == 'prod'
        env:
          OVERLAY: infra/k8s/overlays/prod/fastapi-guide/kustomization.yaml
          SHA: ${{ github.sha }}
        run: |
          sed -i -E "s|(newTag:[[:space:]]*)[A-Za-z0-9._-]+|\1${SHA}|" "$OVERLAY"
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "$OVERLAY"
          if git diff --cached --quiet; then
            echo "newTag 이미 ${SHA} — 커밋 없음"; exit 0
          fi
          git commit -m "ci(prod): bump fastapi-guide image -> ${SHA} [skip ci]"
          # 동시 push 경합(race) 대비 — rebase 후 재시도 (최대 5회).
          # 다른 CD 가 먼저 main 을 옮기면 push 가 non-fast-forward 로 거부되므로,
          # 내 bump 커밋은 유지한 채 origin 을 rebase 로 당겨 위로 얹고 다시 push.
          REF="${{ github.ref_name }}"
          git config pull.rebase true
          for attempt in 1 2 3 4 5; do
            if git push origin "HEAD:${REF}"; then
              echo "✅ overlay 갱신 푸시 — ArgoCD(main 추적)가 롤아웃합니다."
              exit 0
            fi
            echo "⚠ push 거부(경합 추정) — 재시도 ${attempt}/5: origin/${REF} rebase"
            if ! git pull --rebase origin "${REF}"; then
              # 같은 overlay 를 양쪽이 건드려 충돌 → 우리 newTag(${SHA})로 해소
              echo "rebase 충돌 — newTag ${SHA} 로 재적용"
              sed -i -E "s|(newTag:[[:space:]]*)[A-Za-z0-9._-]+|\\1${SHA}|" "$OVERLAY"
              git add "$OVERLAY"
              GIT_EDITOR=true git rebase --continue || git rebase --abort
            fi
            sleep $((RANDOM % 5 + 2))   # 2~6초 지터로 동시 재시도 분산
          done
          echo "✘ 5회 재시도 후에도 push 실패 — 수동 확인 필요"; exit 1