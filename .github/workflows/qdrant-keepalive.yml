# .github/workflows/qdrant-keepalive.yml
on:
  schedule:
    - cron: '0 0 */3 * *'   # 3일마다 (1주 한참 안쪽)
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -fsS -H "api-key: ${{ secrets.QDRANT_API_KEY }}" "${{ secrets.QDRANT_URL }}/collections"