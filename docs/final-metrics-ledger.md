# 발표 지표 원장 (final metrics ledger)

프리즈 시점(2026-07-07) 기준, 발표에 인용 가능한 지표를 **출처·조건·판정**과 함께 정리한다.
목적: 미검증 수치의 발표 유입 차단 + 각 수치의 성격(실측/시연/추정)을 명확히 구분.

**판정 범례** — `실측-repo`(코드/문서에 근거 있음) · `세션측정`(이번 검증 세션에서 실측, 원자료는 산문/외부) · `시연수치`(데모용 합성 데이터) · `미검증`(근거 미확인 → **값 인용 금지**).

| # | 지표 | 값 | 출처 | 조건·시점 | 발표 판정 |
|---|---|---|---|---|---|
| 1 | Grok 프롬프트 캐시 | **원자료 있음(수치 인용 비권장)** | `backend/docs/decisions/grok-prompt-cache-history-trim.md`; 지표원천 `cached_tokens`(`GrokService.java:112-117`) | 단일 run 마이크로벤치. conv-id 기여는 문서가 **자기부정**(OFF 75.0% > ON 71.8%) | ⚠️ **발표 인용 비권장** — 문서가 "절대 'X%→Y%'로 박지 말 것"이라 명시(단일 run 노이즈). 정성적으로 "프롬프트 캐시 적용 + 이력 트림"까지만 |
| 2 | VLM Bedrock 페어드 전환 | 골든셋 **35장·111콜**, **정확도 동급**, **레이턴시 2.8×↓**, **실패 0** | `README.md:99-101`·`fastapi/README.md` 산문; 도구 `fastapi/guide/tools/golden-set-kit/phase2_backend_eval.py` | 동일 코드·백엔드만 Gemini→Bedrock Claude 교체 페어드 비교 | ✅ **세션측정** 사용 가능 — 단 "eval 원자료 JSON은 `/tmp` 산출·미커밋"임을 명기(재현은 골든셋 킷) |
| 2b | 이미지당 비용 $0.0068 · prod 2.2s/콜 | — | in-repo 없음(비용은 `phase2_backend_eval.py`의 **가정 단가**) | — | ⛔ **미검증 — 값 인용 금지** |
| 3 | 가이드 full 생성 ~82.9s | — | in-repo 없음 | — | ⛔ **미검증 — 수치 인용 금지**. 정성적 "가이드 생성은 순차 Grok 코칭이 지배적 → 병렬화 로드맵 후보"(ADR 0008)만 |
| 4 | 레퍼런스 재추천(reroll) | **LLM 0콜 · 벡터검색 1경로 · 무상태** | `fastapi/guide/routes.py:675-687`(docstring "LLM/진단/코칭 0콜, 참조 벡터검색만") | #52. 노출분 exclude 누적, 소진 시 생성 전환 | ✅ **실측-repo** 사용 가능 — "sub-second"는 실측 타이밍 미기록이라 **"LLM 0콜 설계상 저지연"**으로만 |
| 5 | 가이드 코퍼스 규모 | **prod 12,717점** (self_render 11,334=**89.1%** / museum 1,383=**10.9%**, ai_example 0) | 세션 Qdrant 스냅샷 실측(2026-07-07, `reference_images_prod`) | prod=클린 기준선(ai_example 0). dev=12,720(=+3 ai_example 검증 백필) | ✅ **세션측정(스냅샷)** 사용 가능 — 산술 검산 완료(11,334+1,383=12,717) |
| 6 | 성장 지표 '명암 대비' 주 6회→1회 | 주 6회 → 1회(감소) | `README.md:40,64`; 데이터 project 32(`cleanup-manifest` A5/B2/C1) | **합성 8주 감소 이력**(B2: guide_id LIKE 'g32-%', "렌더용 합성") | ⚠️ **시연수치** — "시연용 합성 성장 이력"임을 **반드시 명기**. 실사용 통계 아님 |
| 7 | 채택률·저장률 퍼널 | **집계 구조 구축(수치는 축적 중)** | `backend/.../admin/repository/AdminFunnelRepository.java:20-125`(노출·👍·👎·저장 집계 쿼리 5종) | `:since` 윈도우 라이브 쿼리 | ✅ **구조=실측-repo** / ⛔ **구체 채택률·저장률 값은 미검증**(라이브 DB 실행 필요) — "집계 파이프라인 구축·수치 축적 중"으로 |
| 8 | prod 스모크(릴리스 실증) | **3기능 prod 실증 · pageError 0** | G4-④ 세션 실증(2026-07-07, drawe.xyz) | #52 reroll·#53 로딩/실패 UX·#54 취향 결 뱃지 실렌더, API 4xx/5xx 0 | ✅ **세션측정** 사용 가능 |

## 발표 사용 요약
- **바로 사용 가능(실측/세션측정)**: #2(VLM 페어드 35·111·2.8×·실패0), #4(reroll LLM 0콜), #5(코퍼스 12,717·89.1/10.9), #8(prod 스모크 3기능·pageError 0).
- **조건부·주의**: #6(시연 합성 명기), #7(구조만·수치 축적 중), #1(캐시는 정성 서술만).
- **인용 금지(미검증)**: #2b($0.0068·2.2s/콜), #3(82.9s), #1의 "17.7%→71.7%", #7의 구체 채택/저장 수치.

> 갱신 규칙: 미검증 항목은 **실측 원자료(커밋된 eval JSON·라이브 퍼널 쿼리 결과)** 확보 시에만 값을 채운다. 프롬프트를 건드리는 변경은 골든셋 재평가를 짝으로(README '품질과 성능' 원칙).
